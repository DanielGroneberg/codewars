--# Instructions: You are given a table 'students' with columns 'id', 'name', and 'score'. Write a SQL query to find the names of students who scored above 75.
SELECT name
FROM students
WHERE score > 75;
